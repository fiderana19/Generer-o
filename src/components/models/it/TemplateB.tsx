export const TemplateB: React.FC<{ data: string; }> = ({ data }) => {
  // Utilise les données et le thème pour dessiner la mise en page A (ex: deux colonnes)
  return (
    <div>
      {/* ... Affichage des données. La mise en page est ici. ... */}
      b  ito
    </div>
  );
};